import 'package:flutter/material.dart';

class NewpProductPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.white,
        title: Text(
          'Agregar Producto',
          style: TextStyle(color: Colors.black),
        ),
      ),
      body: Center(
        child: Text(''),
      ),
    );
  }
}
